package com.peng.agent.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ────────────────────────────────────────────────────────────────────
val BrandPrimary = Color(0xFF00B849)        // 翡翠绿 — 主accent
val BrandPrimaryDark = Color(0xFF009A3D)    // 深绿 — 按钮按下态
val BrandPrimaryLight = Color(0xFF4CCE7A)   // 浅绿 — hover/focus态
val BrandContainer = Color(0xFFE8F9EF)      // 极浅绿 — 容器背景

// ── Light Theme Surface ─────────────────────────────────────────────────────
val LightBackground = Color(0xFFF7F8FA)     // 主背景 — 微灰白
val LightSurface = Color(0xFFFFFFFF)        // 卡片/surface — 纯白
val LightSurfaceVariant = Color(0xFFF0F2F5) // 二级surface — 浅灰
val LightElevated = Color(0xFFFFFFFF)       // 浮起surface — 纯白

// ── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFF1A1D23)         // 主文字 — 近黑
val TextSecondary = Color(0xFF6B7280)       // 副文字 — 中灰
val TextHint = Color(0xFF9CA3AF)            // 提示文字 — 浅灰
val TextDisabled = Color(0xFFD1D5DB)        // 禁用文字 — 极浅灰

// ── Semantic ─────────────────────────────────────────────────────────────────
val Success = Color(0xFF00B849)
val Warning = Color(0xFFF59E0B)
val Error = Color(0xFFEF4444)
val Info = Color(0xFF3B82F6)

// ── Outline / Border ────────────────────────────────────────────────────────
val Outline = Color(0xFFE5E7EB)             // 主边框 — 浅灰
val OutlineVariant = Color(0xFFF3F4F6)      // 次边框 — 极浅灰
val Divider = Color(0xFFF3F4F6)             // 分割线

// ── Chat Bubbles ─────────────────────────────────────────────────────────────
val UserBubbleBg = Color(0xFF00B849)        // 用户气泡 — 翡翠绿
val UserBubbleText = Color(0xFFFFFFFF)      // 用户气泡文字 — 白
val AiBubbleBg = Color(0xFFF0F2F5)          // AI气泡 — 浅灰
val AiBubbleText = Color(0xFF1A1D23)        // AI气泡文字 — 近黑

// ── Tool Status ──────────────────────────────────────────────────────────────
val ToolRunning = Color(0xFFF59E0B)
val ToolSuccess = Color(0xFF00B849)
val ToolFailed = Color(0xFFEF4444)

// ── Code Block ───────────────────────────────────────────────────────────────
val CodeBackground = Color(0xFFF6F8FA)
val CodeBorder = Color(0xFFE1E4E8)
val CodeText = Color(0xFF24292E)
